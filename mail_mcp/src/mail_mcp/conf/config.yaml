defaults:
  - app_config_schema
  - _self_

server:
  name: mail-mcp
  transport: streamable-http
  host: 127.0.0.1
  port: 8000
  path: /mcp
  stateless_http: true
  json_response: true

hello:
  greeting: Hello
  default_name: world
